# Copy to .env.migration on the deployment host and chmod 600. This file is
# consumed only by `npm run prisma:migrate:production`; PM2 must not load it.
MIGRATION_DATABASE_URL=postgresql://eduai_migration:replace-me@localhost:5432/eduai
