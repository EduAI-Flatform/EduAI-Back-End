enum UserStatus {
  active
  inactive
  suspended
}

enum RoleName {
  student
  instructor
  platform_admin
}

enum CourseLevel {
  beginner
  intermediate
  advanced
}

enum CourseStatus {
  draft
  published
  archived
}

enum CourseVisibility {
  public
  private
}

enum LessonType {
  video
  pdf
  article
}

enum QuizStatus {
  draft
  published
  archived
}

enum QuestionType {
  multiple_choice
  true_false
  short_answer
}

enum AssignmentStatus {
  draft
  published
  archived
}

enum SubmissionStatus {
  submitted
  graded
}

enum ClassroomSessionStatus {
  scheduled
  live
  ended
  cancelled
}
